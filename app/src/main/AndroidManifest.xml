<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Required: marks this as a TV/Leanback app -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <!-- TV devices may not have touchscreen -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Curated guest apps shown by the Home launcher. Avoid QUERY_ALL_PACKAGES. -->
    <queries>
        <package android:name="com.google.android.youtube.tv" />
        <package android:name="com.viettel.tv360.tv" />
        <package android:name="vn.thvli.androidtv" />
        <package android:name="net.fptplay.ottbox" />
        <package android:name="com.vieon.tv" />
        <package android:name="com.netflix.mediaclient" />
        <package android:name="com.spotify.tv.android" />
        <package android:name="vng.zing.mp3" />
    </queries>

    <application
        android:name=".PhsIptvApp"
        android:allowBackup="false"
        android:banner="@drawable/ic_tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.PhsIptv">

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Makes the app visible in the Android TV app launcher. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Allows the TVBox to select this activity as its default Home. -->
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            Firebase auto-init disabled until google-services.json is present.
            REMOVE this block after adding google-services.json and enabling the
            google-services plugin in app/build.gradle.kts.
        -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            android:exported="false"
            android:initOrder="100"
            tools:node="remove" />

        <!-- Disable FCM auto-init until firebase is configured -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />

    </application>

</manifest>
